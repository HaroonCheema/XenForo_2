<?xml version="1.0" encoding="utf-8"?>
<template_modifications>
  <modification type="public" template="thread_view" modification_key="hide_first_post" description="Hide First Post For Zoom" execution_order="10" enabled="1" action="str_replace">
    <find><![CDATA[<xf:macro name="{{ $templateOverrides.post_macro ?: 'post_macros::post' }}"
							arg-post="{$post}"
							arg-thread="{$thread}"
							arg-highlightedPosts="{$highlightedPosts}"
							args="{$templateOverrides.post_macro_args}" />]]></find>
    <replace><![CDATA[<xf:if is="{{$post.Thread.Meeting && $post.isFirstPost()}}">
	
<xf:else />
$0
</xf:if>]]></replace>
  </modification>
  <modification type="public" template="thread_view" modification_key="replace_with_zoom" description="Replace with zoom" execution_order="10" enabled="1" action="str_replace">
    <find><![CDATA[<xf:breadcrumb source="$forum.getBreadcrumbs()" />]]></find>
    <replace><![CDATA[$0
<xf:if is="{{$thread.Meeting}}">
	
	<xf:css src="general_zoom_meeting.less" />
	<xf:css src="meeting_overiew.less"/>
	<xf:macro template="zoom_meeting_macro" name="header"
		arg-meeting="{$thread.Meeting}" />

	<xf:macro template="zoom_meeting_macro" name="tabs"
		arg-meeting="{$thread.Meeting}"
		arg-selected="discussion" />

</xf:if>]]></replace>
  </modification>
</template_modifications>
