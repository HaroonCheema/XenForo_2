<?xml version="1.0" encoding="utf-8"?>
<template_modifications>
  <modification type="public" template="post_macros" modification_key="StartConversation_post_macros" description="StartConversation_post_macros" execution_order="13" enabled="1" action="str_replace">
    <find><![CDATA[<xf:if is="$hasActionBarMenu">]]></find>
    <replace><![CDATA[<xf:if is="!in_array($thread.node_id, $xf.options.startConversationExcludeForums)">
	<xf:if is="$post.User AND $xf.visitor.canStartConversationWith($post.User)">
		<xf:set var="$hasActionBarMenu" value="{{ true }}" />
		<a href="{{ link('conversations/start-conversation', '', {'to': $post.username, 'title': $post.Thread.title, 'post_id': $post.post_id}) }}" class="actionBar-action actionBar-action--startConversation actionBar-action--menuItem">{{ phrase('startConversation_start_conversation') }}</a>
	</xf:if>
</xf:if>
$0]]></replace>
  </modification>
</template_modifications>
